<%= form_with(model: productz) do |form| %>
  <% if productz.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(productz.errors.count, "error") %> prohibited this productz from being saved:</h2>

      <ul>
        <% productz.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :restaurant_id, "Restaurants" %>
    <%= form.select :restaurant_id, Restaurant.all.map {|restaurant| [restaurant.name, restaurant.id]}, { include_blank: true, prompt: "Select a restaurant" }, class: "form-control" %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :cost, style: "display: block" %>
    <%= form.number_field :cost %>
  </div>

  <div>
  <%= form.submit "Submit", class: "btn btn-warning logo-gr-btn magin-top-btn" %>
  </div>
<% end %>
