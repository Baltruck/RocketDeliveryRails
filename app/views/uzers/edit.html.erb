<div class="container">
  <h1 class="text-center">Editing user</h1>

  <div class="card mx-auto table-custom-head custom-card" style="max-width: 80%;">
    <div class="card-body text-center">
      <%= render "form", uzer: @uzer %>
    </div>
  </div>

  <div class="text-center mt-3">
    <%= link_to "Show this user", @uzer, class: "btn btn-primary logo-or-btn btn-lg me-2" %>
    <%= link_to "Back to users", uzers_path, class: "btn btn-warning logo-gr-btn btn-lg" %>
  </div>
</div>
