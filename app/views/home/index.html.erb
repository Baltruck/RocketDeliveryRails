<% if user_signed_in? %>
  <div class="text-center fs-3 fw-bold"> Welcome <%= current_user.email %> </div>
  <div class="d-flex justify-content-center">
    <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-warning btn-lg m-3" %>
  </div>
<% else %>
  <div class="text-center fs-3 fw-bold">Welcome stranger</div>
  <div class="d-flex justify-content-center">
    <%= button_to "Sign in", new_user_session_path, class: "btn btn-warning btn-lg m-3" %>
  </div>
<% end %>

<h2 class="text-center">Rocket Delivery Staff Reserved Application</h2>
<div class="d-flex justify-content-center">

</div>
